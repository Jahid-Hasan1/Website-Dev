const data= require("./data.json");

// console.log(data.students[0].age);
// data.students[1].age=55;
// console.log(data.students[1].age);
// delete data.students[2].id;
 console.log(data);
// access data usin loop.
// for(x in data){
//   cosole.log(x);
//   cosole.log(data[x]);
// }
// to convert Javascript to JSON: JSON.stringify(variable name)
// to convert JSON file to Javascript: JSON.parse(file name)
//to create schema for json data, search json to schema converter.pwd